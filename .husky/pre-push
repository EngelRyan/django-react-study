#!/usr/bin/env sh
echo "Rodando checagem rápida antes do push..."

# lint no frontend
npm run lint --prefix frontend

# checagem de sintaxe no backend (rápido, não é um linter completo)
cd backend && python -m py_compile $(find . -name "*.py" -not -path "*/migrations/*" -not -path "*/venv/*")
